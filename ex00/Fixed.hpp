#ifndef FIXED_H
#define FIXED_H



class Fixed{
    private :
        int fixedPoint ;
        static const int fractionalNumber ;
    public :
        Fixed();
        ~Fixed();
        Fixed(const Fixed&);
        
};
#endif
